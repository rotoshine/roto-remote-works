# 보안 정책

roto-remote-works는 **실행 중인 앱의 코멘트를 코드 수정으로 바꾸는** 도구라,
신뢰 경계가 설계의 핵심입니다. 이 문서는 위협 모델과 운영 수칙, 제보 방법을 정리합니다.

## 취약점 제보

공개 이슈로 올리지 말고 다음 중 하나로 비공개 제보해 주세요:
- GitHub **Security advisories**(repo → Security → Report a vulnerability), 또는
- 이메일: rotoshine@gmail.com

가능하면 재현 절차, 영향 범위, 관련 커밋/버전을 포함해 주세요. 개인 프로젝트라
SLA는 보장하지 못하지만 확인되는 대로 대응합니다.

## 신뢰 모델

### 두 신뢰 티어 (토큰)
- **`clientToken` (저신뢰, 브라우저)** — 코멘트 작성, 상태/질문 읽기, 질문 답변,
  apply 요청, 코멘트 비우기만 가능.
- **`token` (고신뢰, 서버 측)** — 위 + agent 전용 라우트(코멘트 resolve/patch,
  진행 상태 설정, 질문 등록, 스크린샷 읽기, apply 요청 읽기/삭제). 저신뢰 토큰은
  이 라우트에서 **403**.
- `clientToken`을 설정하지 않으면 단일 `token`이 모든 것을 인가합니다(로컬 기본값).

> **고신뢰 토큰을 브라우저로 보내지 마세요.** 오버레이는 `rrw.config.json`을 번들에
> 포함하므로, 2-토큰 모드에서는 고신뢰 `token`을 파일에서 빼고 브리지/에이전트
> 호스트의 `RRW_TOKEN` 환경변수로만 주세요. 파일에는 `clientToken`만 둡니다.

### 코멘트는 신뢰할 수 없는 데이터
코멘트는 누구나(원격 디자이너/PM 포함) 남길 수 있으므로 **명령이 아니라 데이터로**
취급합니다. 에이전트 프롬프트/프로토콜은 코멘트 본문을 지시로 해석하지 않도록
명시합니다(프롬프트 인젝션 방어). 어댑터를 추가할 때도 이 원칙을 지켜야 합니다.

### apply는 운영자 게이팅
에이전트는 코멘트를 자동으로 적용하지 않습니다. `apply` 요청이 있을 때만 처리하며,
원격-origin 요청은 **검토 후 적용**(worker `delivery=pr`이면 PR로) 흐름을 권장합니다.
동시 적용은 single-flight로 한 번에 한 배치만 처리합니다.

### 네트워크 경계
- 브리지는 기본적으로 **`127.0.0.1`**(loopback)에만 바인드합니다.
- 비-loopback(`0.0.0.0` 등)에 바인드하면 시작 시 **경고**를 출력합니다.
- 원격으로 쓸 때는 **절대 공개 인터넷에 노출하지 말고** Tailscale tailnet이나
  Cloudflare Access 같은 **네트워크 게이팅** 뒤에 두세요(README의 Tailscale 런북).

## 운영 체크리스트
- [ ] 토큰은 충분히 길고 무작위인 값으로(예: `openssl rand -hex 32`).
- [ ] 2-토큰 모드: 고신뢰 토큰은 env로만, 파일/번들엔 저신뢰 토큰만.
- [ ] 원격 브리지는 Tailscale/Cloudflare Access 뒤에서만.
- [ ] worker가 PR을 여는 호스트의 `gh`/git 자격은 최소 권한으로.
- [ ] 오버레이는 **dev 전용**으로 마운트(프로덕션 번들에 포함 금지).
